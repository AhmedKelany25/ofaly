export enum Languages {
  English = 'en',
  Hebrew = 'hr',
  Arabic = 'ar',
  Russian = 'ru',
  French = 'fr',
}

export enum Directions {
  RTL = 'rtl',
  LTR = 'ltr',
}

export enum Gender {
  male = 'male',
  female = 'women',
}

export enum AppointmentState {
  standing = 'standing',
  done = 'done',
  canceled = 'canceled',
}

export enum ActiveTab {
  past = 'past',
  future = 'future',
}
